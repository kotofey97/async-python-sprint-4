version: '3.8'

services:

  web:
    container_name: web
    build: .
    working_dir: /src
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - 8000:8000
    volumes:
      - ./src:/src
